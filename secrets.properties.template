# ============================================================
#  小智 Android 客户端 —— 本地密钥配置【模板】
# ============================================================
#  本模板文件需要提交到版本库，供其他人参考。
#  真实密钥请写入 secrets.properties（该文件不入库）。
#
#  初始化步骤：
#      cp secrets.properties.template secrets.properties
#      然后填入下面的真实值
#
#  说明：
#    - 若 secrets.properties 不存在，项目仍可正常编译，
#      但读取到的密钥为空，对应功能在运行时会不可用。
#    - 模板中只放「键名」和说明，绝不填写真实密钥。
# ============================================================

# --- 腾讯位置服务  https://lbs.qq.com/ ---
# 控制台 -> 应用管理 -> 我的应用 -> 添加 Key
# 下面两个 Key 用途不同，需要在控制台分别勾选各自的服务：

# 1) WebService API Key
#    用途：SearchNaviActivity 调用 /ws/place/v1/search 做 POI 搜索
#    要求：勾选「WebServiceAPI」，并按控制台提示配置签名或 IP 白名单
tencent.map.webserviceKey=

# 2) Android SDK Key
#    用途：地图 SDK 初始化，会写入 AndroidManifest 的 TencentMapSDK meta-data
#    要求：勾选「Android」，并填入应用包名与签名 SHA1
#          包名：com.lhht.xiaozhi
#          签名 SHA1：用 Android Studio 的 signingReport 或
#                     keytool -list -v -keystore <你的签名文件> 获取
tencent.map.sdkKey=
